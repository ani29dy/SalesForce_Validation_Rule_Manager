{
  "name": "validation-rule-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Salesforce Validation Rule Manager — unified Render deploy",
  "scripts": {
    "build:render": "npm install --prefix frontend --include=dev && npm run build --prefix frontend && npm install --prefix backend && node backend/scripts/prepare-public.js",
    "start": "npm start --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
